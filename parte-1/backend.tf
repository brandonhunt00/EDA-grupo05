terraform {
  backend "s3" {
    # Preencher após criar o backend remoto.
    # bucket         = "eda262-gNN-terraform-state"
    # key            = "parte-1/terraform.tfstate"
    # region         = "us-east-1"
    # dynamodb_table = "eda262-gNN-terraform-lock"
    # encrypt        = true
  }
}
